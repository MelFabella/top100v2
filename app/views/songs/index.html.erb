<h1><%= @billboard.title %> Songs</h1>
<%= link_to 'Add Songs', new_billboard_song_path(@billboard) %>
<hr />
<h2>Songs</h2>
<ul>
  <% @songs.each do |s| %>
    <li>
      <%= s.artist.name %>
      <%= s.artist.rank %>
      <%= s.artist.numAlbs %>
      <br />
      <%= link_to 'remove', billboard_song_path(@billboard, s), method: :delete  %>
    </li>
  <% end %>
</ul>
