<%= form_for [@artists, @songs] do |f| %>
  <%= f.label :user %>
  <%= f.select :user_id, @users.collect { |u| [u.full_name, u.id]} %>
  <br />
  <%= f.label :role %>
  <%= f.select :role, ['ta', 'teacher', 'student'] %>

  <%= f.submit %>
<% end %>