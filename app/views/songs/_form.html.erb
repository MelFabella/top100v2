<%= form_for [@billboard, @song] do |f| %>
  <%= f.label :artist %>
  <%= f.select :artist_id, @artists.collect { |a| [a.name, a.rank, a.numAlbs, a.id]} %>
  <br />
  <%= f.label :song %>
  <%= f.select :billboard_id, @billboards%>

  <%= f.submit %>
<% end %>
