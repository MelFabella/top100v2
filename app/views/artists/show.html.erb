<h1><%= @artist.name %></h1>
<hr />
<h2>Billboards</h2>
<ul>
  <% @artist.billboards.each do |billboard| %>
    <li>
      <%= link_to billboard.title, billboard %>
    </li>
  <% end %>
</ul>
<br />
<%= link_to 'Delete Artist', @artist, method: :delete %>